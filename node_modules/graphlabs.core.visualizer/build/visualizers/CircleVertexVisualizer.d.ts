import { IVertexVisualizer } from "../types/IVertexVisualizer";
import { Vertex } from "graphlabs.core.graphs";
import { GeometricVertex } from "../geometrics/GeometricVertex";
export declare class CircleVertexVisualizer implements IVertexVisualizer {
    static calculate(vertex: Vertex, x: number, y: number, radius?: number): GeometricVertex<Vertex>;
}
