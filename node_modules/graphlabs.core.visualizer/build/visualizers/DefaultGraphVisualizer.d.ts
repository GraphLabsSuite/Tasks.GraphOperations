import { IGraph, IEdge, IVertex, Vertex, Edge, Graph } from "graphlabs.core.graphs";
import { IGraphVisualizer } from "../types/IGraphVisualizer";
import { GeometricGraph } from "../geometrics/GeometricGraph";
import { GeometricVertex } from "../geometrics/GeometricVertex";
export declare class DefaultGraphVisualizer implements IGraphVisualizer {
    geometric: GeometricGraph<Graph<Vertex, Edge>, Vertex, Edge>;
    width: number;
    height: number;
    vertices: GeometricVertex<Vertex>[];
    constructor(graph: IGraph<IVertex, IEdge>);
    calculate(): void;
}
