import { Toolbar } from '../Toolbar';
import { IGraph, IVertex, IEdge } from 'graphlabs.core.graphs';
import { Component, SFC } from 'react';
import 'bootstrap/dist/css/bootstrap.css';
import { INGraphsView } from '../../models';
interface State {
    status: boolean;
}
export declare class Template extends Component<{}, State> {
    scc_count: number;
    state: {
        status: boolean;
    };
    componentWillMount(): void;
    constructor(props: {});
    render(): JSX.Element;
    protected graphManager(data: any): IGraph<IVertex, IEdge>;
    protected matrixManager(data: any): any;
    protected nGraphsManager(data: any): INGraphsView;
    protected getTaskToolbar(): typeof Toolbar;
    protected getArea(): SFC<{}>;
    protected task(): SFC<{}>;
    private dispatch;
}
export {};
