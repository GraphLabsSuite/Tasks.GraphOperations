import { IVertex } from "../types/IVertex";
import { IGraph } from "../types/IGraph";
import { IEdge } from "../types/IEdge";
/**
 * Checker of the two graphs isomorphism
 */
export declare class IsomorphismChecker {
    static bijection: {
        [key: string]: string;
    };
    static permute(vertices: IVertex[], pre?: IVertex[][]): IVertex[][];
    static updateBijection(verticesOne: IVertex[], verticesTwo: IVertex[]): void;
    private static compareHelper;
    static directCompare(graphOne: IGraph<IVertex, IEdge>, graphTwo: IGraph<IVertex, IEdge>): boolean;
    static checkIsomorphism(graphOne: IGraph<IVertex, IEdge>, graphTwo: IGraph<IVertex, IEdge>): boolean;
}
