import * as React from 'react';
import { WritableAdapter } from "graphlabs.core.visualizer";
import { IGraph, IEdge, IVertex } from "graphlabs.core.graphs";
export interface GVProps {
    adapterType?: string;
    graph: IGraph<IVertex, IEdge>;
    namedEdges?: boolean;
    vertexNaming?: boolean;
    withoutDragging?: boolean;
    edgeNaming?: boolean;
    incidentEdges?: boolean;
    weightedEdges?: boolean;
}
declare let adapter: WritableAdapter;
export declare class GraphVisualizer extends React.Component<GVProps> {
    render(): JSX.Element | undefined;
}
export { adapter };
