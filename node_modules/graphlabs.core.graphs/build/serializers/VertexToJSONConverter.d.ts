import { IVertex } from "../types/IVertex";
import { VertexJSON } from "../types/VertexJSON";
import { IGraph } from "../types/IGraph";
import { IEdge } from "../types/IEdge";
export declare class VertexToJSONConverter {
    static convert(vertex: IVertex): VertexJSON;
    static convertBack(vertex: VertexJSON, graph?: IGraph<IVertex, IEdge>): IVertex;
}
