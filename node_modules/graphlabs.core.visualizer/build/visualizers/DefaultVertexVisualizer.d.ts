import { Vertex } from "graphlabs.core.graphs";
import { GeometricVertex } from "../geometrics/GeometricVertex";
export declare class DefaultVertexVisualizer {
    static calculate(vertex: Vertex, maxWidth: number, maxHeight: number): GeometricVertex<Vertex>;
    static calculateN(vertices: Vertex[], min: number, max: number): GeometricVertex<Vertex>[];
}
