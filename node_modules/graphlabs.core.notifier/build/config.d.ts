export interface InitNotifier {
    host: string;
    port: number;
    path: string;
    protocol: string;
}
export declare function init(params: InitNotifier): void;
export declare function getConfig(): InitNotifier;
