import { IVertex } from "../types/IVertex";
import { IGraph } from "../types/IGraph";
import { IEdge } from "../types/IEdge";
export declare class GraphCharacteristics {
    /**
     * Get subgraph of graph
     * input: vertives
     */
    static getSubgraph(subVertices: string[], graph: IGraph<IVertex, IEdge>): IGraph<IVertex, IEdge>;
    /**
     * Get neighbourhood
     */
    static getNeighbourhood(vertex: string, graph: IGraph<IVertex, IEdge>): string[];
    /**
     * Get neighbourhood
     */
    static getNonNeighbourhood(vertex: string, graph: IGraph<IVertex, IEdge>): string[];
}
